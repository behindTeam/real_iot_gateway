package com.front;

import org.json.simple.JSONArray;
import org.json.simple.JSONObject;

public class ConfigureSettings {
    JSONObject object;

    public ConfigureSettings(JSONObject object) {
        this.object = object;
    }

    JSONArray array = object.
}
